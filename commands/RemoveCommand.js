class RemoveCommand {
    constructor() {
        this.oldArgs = null
        this.created = null
    }

    model () {
        throw new Error("Needs to be overriden")
    }
    
    execute (o) {
        this.oldArgs = o
        
        this.created = Objeco this.
        this.created = nullthis.created = Object.assign({}, this.model().save(o))
        return this.created;this.model().save(Object.assign({}, this.n this.createdeleteById {id:oldArgs.id)